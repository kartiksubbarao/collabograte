<?php

require_once 'extensions/LdapAuthentication/LdapAuthentication.php';

$wgAuth = new LdapAuthenticationPlugin();
$wgLDAPDomainNames = array('LDAP');
$wgLDAPServerNames = array('LDAP' => 'localhost');
$wgLDAPSearchAttributes = array('LDAP' => 'uid');
$wgLDAPBaseDNs = array( 'LDAP' => '<%= scope.lookupvar('collabograte_common::ldap_basedn') %>');
# This can be changed when we set up certificates
$wgLDAPEncryptionType = array('LDAP' => 'clear');

?>
